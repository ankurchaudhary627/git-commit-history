import { useState } from 'react'

const Counter = (props) => {
  // const { } = props
  const [timer, setTimer] = useState(30)
  return (
    <div>
      Counter placeholder
    </div>
  )
}

export default Counter
